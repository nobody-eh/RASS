# E17: DL3DV second method — splatfacto/3DGS (Task P17)

**Status: RUNNING** (camera-ready material; no rebuttal text depends on it).

- Pipeline: the E13 streamed design with nerfacto swapped for splatfacto
  (nerfstudio's 3DGS). `ns-train splatfacto` defaults, `--machine.seed 0`,
  downscale 4 (960P), per-scene PSNR/SSIM/LPIPS in the E13 log schema with
  a provenance field marking it camera-ready material. Per-scene
  checkpointing; resumable (scene done == JSON exists); runs at `nice 10`,
  low priority relative to interactive work; GPU shared with the resumed
  n5k360 nerfacto training worker.
- Incident log: the first launch (2026-07-26 ~13:00) started during an HF
  API rate-limit storm and errored through all 140 scenes at 0/140 in six
  minutes. Fixed by adding a download retry guard (4 attempts, 180 s
  incremental backoff); relaunched cleanly.
- On completion: rerun the E13 audit as a two-method DL3DV joint event
  (nerfacto + splatfacto) under the same declared contract extended
  method-wise; record as camera-ready material, clearly separated from
  discussion-period results (`E17.audit`, currently null).
- If the discussion requires an update comment and the 140 splatfacto
  scenes happen to be done, flag it — but it is promised nowhere.

Artifacts: `rebuttal_results.json["E17"]` (progress, logs_path, audit),
script `scripts/rebuttal_e17_dl3dv_splatfacto.py`, logs scratchpad
`dl3dv_splatfacto_logs/json`.

# E15: DL3DV contract-declaration evidence, disclosures, event configs (Task P15)

## Step 1 — evidence audit. VERDICT: (a), with scope

**REPORT TO THE DRAFTING SESSION BEFORE POSTING:** the sentence "the audit
contract was declared before any result was computed" is supported for the
core contract and may be posted, PROVIDED the two labeled post-hoc additions
are never claimed as pre-declared.

Timeline (full trail with hashes in `rebuttal/dl3dv_contract_declaration.json`):

| Event | Timestamp | Source |
|---|---|---|
| Core contract declared (k=4, E3c 7.98% rule, KS critical-distance, balanced, M=400, seed 0) | 2026-07-24 22:53 UTC | session transcript line 1487 (task P13), sha256 cb2eaedb… |
| Contract recorded in E13.md + results JSON with `"audit": null` | 2026-07-24 23:16 UTC | transcript line 1596, sha256 709d125a… |
| First DL3DV scene log | 2026-07-25 01:38 local | JSON mtime |
| Last scene log | 2026-07-26 11:32 local | JSON mtime |
| First audit statistic | 2026-07-26 ~11:37 local | audit script run |

Scope notes, stated plainly:
- Evidence is local (session transcript, file mtimes, the recorded
  `audit: null` placeholder); no externally anchored timestamp (pushed
  commit) existed at declaration time — `rebuttal/` was untracked in git.
- **Post hoc, labeled**: (i) proportional + uniform companion frontiers,
  added after the balanced frontier was computed; (ii) the concrete budget
  grid (sizes 8–80), fixed at audit-script authoring after log generation
  but before any audit result.
- Corroborating: the validation gate rejected a mis-wired population on its
  first run (73/400 ≠ 88/400 → STOP), demonstrating the gate was active.

## Step 2 — artifact disclosures (added)

- `rebuttal/dl3dv_audit_card.json`: KS critical distances at N=140 are
  loose (0.19–0.49 across swept sizes) so the binding constraints are the
  dispersion-matched means; the E13 caveat list verbatim (single method,
  self-generated logs, dataset choice postdates submission, 3DGS queued for
  camera-ready); the 3,521 erratum.
- `README.md` new section "Rebuttal Artifacts (Discussion Period)" with the
  erratum and the DL3DV disclosures.

## Step 3 — versioned event configs (schema rass-event-config/1.0)

Nine configs in `rebuttal/event_configs/`: threemethod_equal,
threemethod_proportional, fourmethod (I4=2,915), fivemethod (I5=2,228),
ranking_variantA, ranking_variantB, dispersion_matched (post-hoc label
embedded), regime_constrained_c1, regime_constrained_c2 — each with
population definition, constraints, tolerances, allocation, M, seed rule.

Artifacts: `rebuttal_results.json["E15"]`,
`rebuttal/dl3dv_contract_declaration.json`, `rebuttal/dl3dv_audit_card.json`,
`rebuttal/event_configs/*.json`, README section.

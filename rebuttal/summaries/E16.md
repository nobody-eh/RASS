# E16: anonymous repository sync (Task P16)

**Commit `d50bf31` pushed to github.com/nobody-eh/RASS** (author and
committer `nobody-eh <nobody-eh@users.noreply.github.com>`); 223 files.

## License gate (DL3DV) — PROCEED

DL3DV-10K Terms of Use + CC BY-NC 4.0 (License.md in the gated HF repo).
ToU clause 4 prohibits sharing *the Dataset*; we release only our own
derived per-scene metric scalars and descriptor statistics — no images,
colmap data, or any Dataset inputs. Non-commercial research context; DL3DV
attributed in `rebuttal/method_logs/README.md`. `benchmark-meta.csv` is
DL3DV content and is NOT redistributed (scripts document obtaining it from
the gated repo, license auto-grant).

Bonus re-verification: top-level listings of BOTH
`DL3DV/DL3DV-Benchmark` and `DL3DV/DL3DV-10K-Benchmark` contain only
License.md / README.md / benchmark-meta.csv / download.py + 140 scene dirs.
No per-scene metric files exist anywhere, despite the dataset card's
"performances of methods" phrasing — the E7/E13 gate verdict is
definitively confirmed a third time.

## Inclusions (all live on the remote)

1. All rebuttal scripts (`scripts/rebuttal_*.py`) incl. P0 validation,
   scratch paths parameterized via `RASS_SCRATCH`.
2. Nine versioned event configs (`rebuttal/event_configs/`, E15 step 3).
3. FL-36′ scene list + audit card with the reimplementation provenance
   label intact (`subsets/fl36/`).
4. DL3DV: 140 per-scene metric JSONs (`rebuttal/method_logs/dl3dv_nerfacto/`),
   descriptor CSV, contract declaration, audit card, audit outputs in the
   results JSON.
5. Erratum (3,521) in README and both audit cards.
6. Nutrition5k nerfacto (2,963 rows) + bionerf (2,620 rows) metrics-only
   CSVs, sources anonymized ("additional per-scene method logs").
7. Selection-time artifacts backing E12 (`rebuttal/selection_artifacts/`).

## Claims checklist — 33/33 live

Every "released / ships / we release / audited configuration" claim maps
to a repository path; full two-column table in
`rebuttal_results.json["E16"].claims_checklist`. No STOPs.
NOTE for the drafting session: the checklist covers the claims staged in
the summaries and hold-ready replies; re-run the mapping against the FINAL
posted text before posting.

## Anonymity scrub — zero residual hits

Swept: author/collaborator names, cluster hostnames + account IDs, group
ID, other members' IDs, cluster paths, local username/hostname, home and
scratchpad absolute paths, tokens/passwords, session paths. Actions:
scripts parameterized; cluster references genericized (account-A/B,
the-group, "the cluster login node", `<cluster>`); recovered logs released
metrics-only with checkpoint paths dropped; pre-scrub handoff zip excluded.
Final grep over all staged blobs: **0 hits**. Commit identity verified
anonymous before push.

Artifacts: `rebuttal_results.json["E16"]`.

# E12: why 48 scenes when the plain audit passes at 36 (Task P12)

Investigation only; no rebuttal text changes. **Verdict: (a) documented
reason found**, on two levels, with the tune/test gap recomputed and closed.

## The documented chain

1. **The release decision evaluated 36 — and it failed.** The selection-time
   sweep (`sweep_cluster_k/budget_sweep_k6_auto_v2/budget_sweep_summary.csv`)
   scored 36 scenes at joint pass rate 0.060, **LCB 0.0406 < 0.08**; 48
   scenes was the smallest passing budget (0.1075, LCB 0.0808). The rule is
   encoded in `budget_recommendation.json` (selection metric
   `joint_pass_rate_lcb`, `max_metric_fallback`, `max_total_subset: 48`).
   Note the selection-time event differed from the paper's formal audit: it
   included cross-method mean-gap components, hence its much lower rates.
2. **The paper's frontier never evaluated 36.**
   `scripts/generate_strong_accept_results.py:40` sets
   `DEFAULT_BUDGETS_PER_CLUSTER = [8, 10, 12, 16, 20]` — the released
   frontier grid starts at 48 scenes. The paper's language is precisely
   "lowest-cost **evaluated** screening subset" (main.tex:108) and "smallest
   configuration meeting p_min = 0.08 is b=8" over that grid (main.tex:432)
   — accurate as written. The 36-scene pass (58/400, LCB 0.114) first
   appeared in the rebuttal-period extended sweep.

## Tune/test recomputation (step 2)

The v3 holdout artifacts record only the 48-scene row, so the 36-scene
status was recomputed with the current machinery (per-split populations and
references, M = 400, seed 0):

| Split | pop | 36 scenes | 48 scenes |
|---|---|---|---|
| tune | 704 | 56/400, LCB 0.1094 | 70/400, LCB 0.1409 |
| test | 705 | 62/400, LCB 0.1228 | 69/400, LCB 0.1386 |

Under the **current** event, 36 passes both splits — confirming the 48
release traces to the selection-time event and the evaluated grid, not to a
holdout failure.

## Hold-ready reply (346 chars, verdict a)

> The rule is recorded: the selection-time sweep
> (budget_recommendation.json) evaluated {12,24,36,48}; 36 scored LCB 0.041
> (<0.08), 48 was the smallest passing (LCB 0.081). The paper frontier
> evaluates budgets from 48 upward, hence 'lowest-cost evaluated'. The
> revision adds the current audit's 36-scene row (LCB 0.114) and reconciles
> the frontier.

(Drafts for verdict classes (b) and (c) are stored in the JSON in case a
reviewer contests the framing.)

Artifacts: `rebuttal_results.json["E12"]` (verdict, evidence paths with
quotes, recomputed split statuses, all three reply drafts).

# E11: dispersion-matched operating point (Task P11)

**Label (mandatory on every use): "dispersion-matched operating point,
declared post hoc during the discussion period." Released ALONGSIDE the
default contract, never replacing it.**

Derivation (recorded in JSON): anchor c = tau_PSNR/sigma_PSNR = 0.5/6.2681
= 0.079769 (E3c stds) → tau_SSIM′ = 0.013434, tau_LPIPS′ = 0.017900;
tau_PSNR (0.5 dB) and KS (0.14) unchanged. Calibration cap verified:
0.0134 < 0.0199 (min SSIM cross-method gap), 0.0179 < 0.0952 (min LPIPS
gap). Population, references, seeds, M identical to E1.

## One-sentence result (154 chars, fits the rebuttal slot)

> under it the three-method joint event gives 27/400 at 48 scenes and
> reaches the 0.08 LCB target at 60 scenes with equal allocation (48 with
> proportional).

## a/b. Frontiers (n_pass/400, Wilson LCB)

| Scenes | 24 | 36 | 48 | 60 | 72 | 84 | 96 | 120 |
|---|---|---|---|---|---|---|---|---|
| Equal | 1 | 26 | 27 | **50 (.096)** | 76 | 73 | 104 (.219) | 109 (.231) |
| Proportional | 0 | 22 | **54 (.105)** | 107 (.227) | 131 | 157 | 204 (.461) | 242 (.556) |

Targets: equal → 0.08 at **60**, 0.20 at **96**; proportional → 0.08 at
**48**, 0.20 at **60**. (Default-tolerance contrast: equal never reaches
0.08 ≤ 120 (E1); proportional reaches it at 72 (E10).)

## c. Exported subsets under the new contract

- **RASS-48: still FAILS, and only on Feature-Splatting LPIPS** — exactly
  as the known gaps predicted (mean 0.0211 > 0.0179; KS 0.1651 > 0.14).
  The other 15 constraints pass. Honest expectation stated in the task,
  confirmed by the run.
- **RASS-96: PASSES all 16 constraints.**

## d. Zip-NeRF-only twin frontier (3,521-scene population)

36 scenes: 102/400 (LCB 0.215) — vs 58/400 (0.114) under the default
contract; 48 scenes: 143/400 (0.312). The 0.08 target is reached at 36
scenes as before (24 scenes just misses at LCB 0.0765). The paper's own
frontier has a healthy dispersion-matched twin.

## e. Binding constraints at 48 scenes (equal allocation, 373 failures)

LPIPS no longer dominates: the top constraints are now nearly uniform
across metrics — zipnerf.lpips.mean 203, feature_splatting.ssim.mean 198,
zipnerf.psnr.mean 197, feature_splatting.lpips.mean 182, zipnerf.ssim.mean
180, feature_splatting.psnr.mean 164 — compared with the default contract
where the two LPIPS means led by a wide margin (281/269, E1). The
dispersion-matching did what it was designed to do: equalize how hard each
constraint binds.

## Caveats

- Post-hoc declaration is disclosed in the label; the default contract's
  results (E1, E10) are always reported first.
- KS guardrail intentionally untouched; RASS-48's FS-LPIPS KS failure is
  therefore contract-independent.
- Seeds identical to E1 (default_rng(0 + b)).

Artifacts: `rebuttal_results.json["E11"]` (derivation, cap checks, both
frontiers, RASS tables, zip-only twin, histogram), script
`scripts/rebuttal_e11_dispersion_matched.py`.

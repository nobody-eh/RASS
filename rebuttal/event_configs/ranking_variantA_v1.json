{
  "schema_version": "rass-event-config/1.0",
  "event": "E1 joint event AND sign preservation of all 6 pairwise method mean gaps (3 pairs x {PSNR, SSIM}) (E2 variant A)",
  "population": {
    "definition": "E1 intersection",
    "n_scenes": 3473
  },
  "constraints": {
    "method_metrics": {
      "zipnerf": [
        "psnr",
        "ssim",
        "lpips"
      ],
      "feature_splatting": [
        "psnr",
        "ssim",
        "lpips"
      ],
      "instant_ngp_fi": [
        "psnr",
        "ssim"
      ]
    },
    "ranking": "sign(subset gap) == sign(population gap) for every method pair and metric"
  },
  "tolerances": {
    "psnr": 0.5,
    "ssim": 0.01,
    "lpips": 0.01,
    "ks": 0.14
  },
  "allocation": {
    "type": "equal-per-regime",
    "regimes": "k=6 KMeans seed 0 on the 57-D descriptors excluding proxies num_images/num_frames_total/num_frames_used (shipped mapping, replication validated ARI=1.0)"
  },
  "M": 400,
  "seed_rule": "default_rng(0 + b), identical paired draws with E1"
}
